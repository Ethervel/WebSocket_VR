{
  "name": "vr-meeting-rooms-server",
  "version": "1.0.0",
  "description": "WebSocket server for VR Meeting Rooms application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --runInBand --forceExit"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "pdf-poppler": "^0.2.3",
    "uuid": "^9.0.1",
    "ws": "^8.14.2"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
